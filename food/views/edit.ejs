<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit <%= food.name %></title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <h1>Edit <%= food.name %></h1>
  <form action="/foods/<%= food.id %>?_method=PUT" method="POST">
    <label for="name">Name:</label><br />
    <input type="text" id="name" name="name" value="<%= food.name %>" required /><br />
    <label for="description">Description:</label><br />
    <textarea id="description" name="description" required><%= food.description %></textarea><br />
    <label for="price">Price:</label><br />
    <input type="number" id="price" name="price" value="<%= food.price %>" step="0.01" required /><br /><br />
    <button type="submit">Update Food</button>
  </form>
  <a href="/foods/<%= food.id %>">Cancel</a>
</body>
</html>